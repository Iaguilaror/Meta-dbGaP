< ../config.mk

all:VQ:
	bin/targets | xargs mk

results/sample_indexes.txt:Q:
##	set -x
	mkdir -p `dirname $target`
	wget -q -O - $BASE_URL/ \
	|  grep -v "^<" \
	| grep "/phs" \
	| cut -d'"' -f2 > $target.build \
	&& mv $target.build $target
